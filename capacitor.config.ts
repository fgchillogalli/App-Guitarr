import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bwg.braille',
  appName: 'internetcosas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
